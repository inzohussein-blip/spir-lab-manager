import type { ReactNode } from "react";

export const metadata = { title: "إدارة الرموز", robots: { index: false, follow: false } };

export default function LicensesLayout({ children }: { children: ReactNode }) {
  return children;
}
